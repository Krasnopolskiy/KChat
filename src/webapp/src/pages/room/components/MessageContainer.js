import {Stack} from "@mui/material";
import {ContentBoxFluid} from "../../../components/ContentBoxes";
import * as React from "react";

export const MessageContainer = () =>
        <ContentBoxFluid sx={{height: "inherit"}}>
            <Stack gap={2}>

            </Stack>
        </ContentBoxFluid>
